<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.daggerarchitecture.android.clean">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:name="com.daggerarchitecture.android.clean.application.WikiApplication"
        android:allowBackup="true"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher_round"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.DeviceDefault">
        <activity android:name="com.daggerarchitecture.android.clean.ui.splashscreen.Splashscreen"
            android:theme="@style/Splashscreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.daggerarchitecture.android.clean.ui.homepage.HomepageActivity" />
        <activity android:name="com.daggerarchitecture.android.clean.ui.search.SearchActivity" />
    </application>

</manifest>